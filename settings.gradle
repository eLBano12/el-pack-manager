pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = "https://maven.fabricmc.net/" }
    }
}

rootProject.name = "el-pack-manager"

include(":common")
include(":fabric")
include(":neoforge")
