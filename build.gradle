allprojects {
    group = "com.elbano12.elpackmanager"
    version = "0.1.0"
}

subprojects {
    repositories {
        mavenCentral()
    }

    tasks.withType(JavaCompile).configureEach {
        options.encoding = "UTF-8"
    }
}
